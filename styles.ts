import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {},
  iconContainer: {
    position: "absolute",
    top: 20,
    alignItems: "center",
  },
  iconText: {
    fontSize: 12,
  },
});
